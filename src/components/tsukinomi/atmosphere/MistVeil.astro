<div class="mist-veil" aria-hidden="true">
  <span class="mist-veil__wash"></span>
  <span class="mist-veil__fog mist-veil__fog--near"></span>
  <span class="mist-veil__fog mist-veil__fog--far"></span>
</div>

<style>
  .mist-veil {
    position: fixed;
    inset: 0;
    z-index: 0;
    overflow: hidden;
    pointer-events: none;
  }

  .mist-veil__wash,
  .mist-veil__fog {
    position: absolute;
    inset: -18%;
  }

  .mist-veil__wash {
    background:
      radial-gradient(circle at 16% 8%, rgba(224, 163, 106, 0.16), transparent 34rem),
      radial-gradient(circle at 82% 24%, rgba(201, 134, 72, 0.1), transparent 28rem),
      linear-gradient(180deg, rgba(95, 65, 37, 0.22), rgba(20, 17, 12, 0.02) 42%, rgba(20, 17, 12, 0.78));
  }

  .mist-veil__fog {
    background:
      radial-gradient(ellipse at 20% 46%, rgba(232, 220, 196, 0.075), transparent 34rem),
      radial-gradient(ellipse at 72% 18%, rgba(224, 163, 106, 0.06), transparent 30rem),
      radial-gradient(ellipse at 62% 82%, rgba(92, 80, 66, 0.08), transparent 36rem);
    filter: blur(18px);
    opacity: 0.82;
  }

  .mist-veil__fog--near {
    animation: mist-veil-drift 60s ease-in-out infinite alternate;
  }

  .mist-veil__fog--far {
    opacity: 0.42;
    transform: translate3d(-5%, 3%, 0) scale(1.16);
    animation: mist-veil-drift-far 84s ease-in-out infinite alternate;
  }

  :global(body[data-section="twilight"]) .mist-veil__wash {
    background:
      radial-gradient(circle at 22% 0%, rgba(201, 134, 72, 0.12), transparent 26rem),
      radial-gradient(circle at 76% 18%, rgba(74, 107, 126, 0.2), transparent 34rem),
      linear-gradient(180deg, rgba(62, 56, 48, 0.2), rgba(20, 17, 12, 0.1) 42%, rgba(20, 17, 12, 0.82));
  }

  :global(body[data-section="warm-room"]) .mist-veil__wash {
    background:
      radial-gradient(circle at 44% 14%, rgba(224, 163, 106, 0.2), transparent 32rem),
      radial-gradient(circle at 12% 62%, rgba(201, 134, 72, 0.11), transparent 30rem),
      linear-gradient(180deg, rgba(62, 39, 20, 0.16), rgba(20, 17, 12, 0.08) 46%, rgba(20, 17, 12, 0.78));
  }

  :global(body[data-section="snow-pact"]) .mist-veil__wash {
    background:
      radial-gradient(circle at 18% 0%, rgba(74, 107, 126, 0.1), transparent 32rem),
      radial-gradient(circle at 78% 12%, rgba(110, 39, 48, 0.065), transparent 28rem),
      linear-gradient(180deg, rgba(28, 39, 46, 0.14), rgba(20, 17, 12, 0.08) 44%, rgba(20, 17, 12, 0.9));
  }

  :global(body[data-section="winter-light"]) .mist-veil__wash {
    background:
      radial-gradient(circle at 28% 0%, rgba(173, 192, 195, 0.16), transparent 30rem),
      radial-gradient(circle at 82% 26%, rgba(224, 163, 106, 0.08), transparent 24rem),
      linear-gradient(180deg, rgba(42, 61, 72, 0.2), rgba(20, 17, 12, 0.08) 42%, rgba(20, 17, 12, 0.76));
  }

  :global(body[data-section="twilight"]) .mist-veil__fog,
  :global(body[data-section="winter-light"]) .mist-veil__fog {
    background:
      radial-gradient(ellipse at 24% 44%, rgba(180, 204, 210, 0.062), transparent 34rem),
      radial-gradient(ellipse at 76% 18%, rgba(74, 107, 126, 0.075), transparent 30rem),
      radial-gradient(ellipse at 64% 82%, rgba(92, 80, 66, 0.07), transparent 36rem);
  }

  :global(body[data-section="snow-pact"]) .mist-veil__fog {
    background:
      radial-gradient(ellipse at 24% 44%, rgba(180, 204, 210, 0.032), transparent 34rem),
      radial-gradient(ellipse at 76% 18%, rgba(74, 107, 126, 0.04), transparent 30rem),
      radial-gradient(ellipse at 64% 82%, rgba(92, 80, 66, 0.045), transparent 36rem);
    opacity: 0.5;
  }

  @keyframes mist-veil-drift {
    from {
      transform: translate3d(-2%, 0, 0) scale(1);
    }

    to {
      transform: translate3d(3%, -2%, 0) scale(1.05);
    }
  }

  @keyframes mist-veil-drift-far {
    from {
      transform: translate3d(3%, -2%, 0) scale(1.12);
    }

    to {
      transform: translate3d(-4%, 4%, 0) scale(1.2);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .mist-veil__fog--near,
    .mist-veil__fog--far {
      animation: none;
    }
  }
</style>

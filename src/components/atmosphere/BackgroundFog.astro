<picture class="background-fog" aria-hidden="true">
  <source srcset="/assets/images/hero-forest.avif" type="image/avif" />
  <source srcset="/assets/images/hero-forest.webp" type="image/webp" />
  <img src="/assets/images/hero-forest.jpg" alt="" loading="eager" decoding="async" />
</picture>

<style>
  .background-fog {
    position: fixed;
    inset: 0;
    z-index: -10;
    overflow: hidden;
    background: var(--paper);
    pointer-events: none;
  }

  .background-fog img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    filter: brightness(0.35) saturate(0.6) blur(2px);
    transform: scale(1);
    animation: hvitveldt-ken-burns 90s ease-in-out infinite;
  }

  @keyframes hvitveldt-ken-burns {
    0%,
    100% {
      transform: scale(1);
    }

    50% {
      transform: scale(1.06);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .background-fog img {
      animation: none;
    }
  }
</style>
